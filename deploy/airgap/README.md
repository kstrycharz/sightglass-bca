# Air-gap bundle

`make airgap-bundle` produces a single tarball of images, wheels, and tool
binaries for installation on a network-isolated host. Scheduled M6.

Acceptance for M6 is that this tarball alone installs and runs a full scan on a
host with no network access.
