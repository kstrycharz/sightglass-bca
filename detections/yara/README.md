# YARA rules

Binary pattern rules, loaded alongside the regex packs in the parent directory.
Used where structure matters more than a string match: embedded PKCS#12 blobs,
key headers, certificate DER, packer signatures.
